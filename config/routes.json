{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "store-response-times.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/endPoints",
      "handler": "store-response-times.endPoints",
      "config": {
        "policies": []
      }
    }
  ]
}
